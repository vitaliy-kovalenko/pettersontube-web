import { createMuiTheme } from '@material-ui/core'

const theme = createMuiTheme({
	palette: {
		type: 'dark',
		text: {
			primary: '#fff',
			secondary: '#fff',
		},
		primary: {
			main: '#fff',
		},
	},
})

export default theme
